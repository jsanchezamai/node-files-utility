{
  "name": "node-files-utility",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && ts-node-dev src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run dev",
    "cortadora": "npm run build && ts-node-dev src/cortadora/cortar",
    "lichesseadora": "npm run build && ts-node-dev src/lichesseadora/lichessear"
  },
  "author": "Martín Santomé on behalf of Ox Santomé",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.8.9",
    "@types/ws": "^8.5.8",
    "ts-loader": "^9.5.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "rxjs": "^7.8.1"
  }
}
